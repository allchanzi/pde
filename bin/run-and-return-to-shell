#!/usr/bin/env bash
set -u

shell="${SHELL:-/bin/zsh}"

resume_shell() {
  trap - EXIT HUP INT TERM
  exec "$shell" -l
}

trap resume_shell EXIT HUP INT TERM

if [[ $# -eq 0 ]]; then
  exit 0
fi

"$@"
